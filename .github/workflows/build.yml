name: Build Signed Yaadavar APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Dependencies
        run: npm install

      - name: Prebuild Native Android Project
        run: npx expo prebuild --platform android

      - name: Build Signed Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/../release.jks \
            -Pandroid.injected.signing.store.password=yaadavar123 \
            -Pandroid.injected.signing.key.alias=yaadavar_key \
            -Pandroid.injected.signing.key.password=yaadavar123

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: YaadavarApp-Signed-APK
          path: android/app/build/outputs/apk/release/*.apk

